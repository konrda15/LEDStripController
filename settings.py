class ChSettings:
	def __init__(self, variation, speed, colors):
		self.variation = variation
		self.speed = speed
		self.colors = colors
	
class ModeSettings:
	def __init__(self, mode, brightness):
		self.mode = mode
		self.brightness = brightness
